{
    "buildCommand": "cd frontend && npx quasar build",
    "outputDirectory": "frontend/dist/spa",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}